module.exports = {
    // * If true, only bot owners can call commands
    dev: true,

    token: '',

    prefix: '',

    database: {
        host: '',
        port: 3306,
        user: '',
        password: '',
        database: '',
        charset: 'utf8mb4_general_ci'
    },

    // * Array of user ID's
    owner: [],

    channels: {
        reactionRoles: ''
    }
}